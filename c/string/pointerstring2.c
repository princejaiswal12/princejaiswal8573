#include<stdio.h>
#include<string.h>
int main(){
     char str[]="collage wallah";//str will be same 
    // char* ptr =str;
    // ptr = "physics wallah"; //ptr will change from str to "physics wallah"
    printf("%s",str);
    
    return 0;
}