#include<iostream>
using namespace std;
int main(){
   int x;
   cout<<"Enter the  number  : " ;
   cin>>x;
   
   if (x>99 && x<1000)//can write (and or &&)
   {
    cout<<"Number is three digit";
   }
   else
   {
    cout<<"Number is not of three digit";
   }
  
}